# static-site-generator

Reads from the content and static directories to generate HTLM in the docs directory.

The Markdown being used is currently a placeholder to demonstrate how the generator works.
